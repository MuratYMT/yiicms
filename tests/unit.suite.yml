# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.
class_name: MyUnitTester
modules:
    enabled:
        - Asserts
        - Db
        - Yii2:
              part: [orm, fixtures]